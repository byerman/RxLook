<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.huaweisoft.retrofitdemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.RetrofitActivity"/>
        <activity android:name=".activities.RxJavaActivity"/>
        <activity android:name=".activities.CreateOperActivity"/>
        <activity android:name=".activities.ConverionActivity"/>
        <activity android:name=".activities.MergeOperActivity"/>
        <activity android:name=".activities.FunctionOperActivity"/>
    </application>

</manifest>